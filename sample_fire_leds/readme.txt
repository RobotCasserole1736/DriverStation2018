accepts serial @ 118200 baud

Send format: per led: "r,g,b,"

Terminate with newline.

After newline, characters will be parsed. r,g,b should be integers in range 0-255 inclusive.

Other particle files are just for reference.